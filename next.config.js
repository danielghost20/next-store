/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['https://api.escuelajs.co/api/v1/', 'picsum.photos', 'encrypted-tbn0.gstatic.com', 'placeimg.com']
    }
}

module.exports = nextConfig
